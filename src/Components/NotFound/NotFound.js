import React from 'react';
import Footer from '../shared/Footer';
import Header from '../shared/Header';

const NotFound = () => {
    return (
        <div>

            <h1 style={{ fontSize: "5rem", height: '50vh', paddingTop: '10vh' }} className="text-center text-danger">404 Sorry the page not found!!</h1>

        </div>
    );
};

export default NotFound;